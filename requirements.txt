# Core dependencies
robotframework>=7.0
faker
PyYAML
requests
openai>=1.0
rapidfuzz>=3.0.0
openapi-schema-validator>=0.7.0
colorama>=0.4.6

# Optional extras (A) regex, (B) external $ref, (C) explore mode
# Install with: pip install -e ".[extras]"
rstr>=2.0.0                    # A) regex generování
prance>=0.24.0                 # B) externí $ref resolving
hypothesis-jsonschema          # C) explore režim pro property-based testing
openapi-spec-validator>=0.7.0  # Validation backend pro prance

# Database support (optional)
# Install with: pip install -e ".[db]"
psycopg2-binary>=2.9.0

# Development dependencies
pytest>=7.0
pytest-mock>=3.10
responses>=0.23
flake8
black
