# Testovací konfigurace pro en_US locale
# Hodnoty api_key jsou pouze testovací placeholdery — NEPOUŽÍVAT v produkci.
talosforge:
  locale: en_US
  ai:
    provider: openai
    openai:
      api_key: test-openai-key-do-not-use
      model: gpt-4
    zhipu:
      api_key: test-zhipu-key-do-not-use
      model: glm-4.6
  cache:
    enabled: true
    ttl: 3600
